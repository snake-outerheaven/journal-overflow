#include "vector.h"
#include <stdio.h>

/*
        Trying to implement a fuil fledged task manager in C,

        In this interation, the objective will be to implement a CRUD console
        application

        Where all tasks will be put in a doubly linked list, so the program will
        will just do CRUD operations on it.

        Of course, since its C, lets build the abstractions from the ground up.
*/

int main()
{
    printf("Hello World!\n");
    return 0;
}
